// botfather 处获得的 Bot Token，需要保留引号
const BOT_TOKEN = ''

// 使用者数字ID，不用保留引号
const ME = ''

// 用户发送 /start 时会收到的消息, 可留空
const WELCOME = `*Hello~*`

// 发送成功回执 [ true, false]
const NOTICE = false

module.exports = ENV = {
    WELCOME,
    ME,
    BOT_TOKEN,
    NOTICE
}
